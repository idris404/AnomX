# Kafka JSON stream — replays sample_csv observations as events
name: kafka_sample
source_type: kafka_json
bootstrap_servers: 127.0.0.1:19092
topic: anomx.observations
group_id: anomx-demo
timestamp_field: timestamp
value_field: value
auto_offset_reset: earliest
